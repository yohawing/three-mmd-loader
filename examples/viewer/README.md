# MMD Viewer Example

Run from the repository root:

```sh
npm run example:viewer
```

Open <http://127.0.0.1:4173/examples/viewer/>. The example loads built files
from `dist`, so the script runs `npm run build` before starting the static
server.
