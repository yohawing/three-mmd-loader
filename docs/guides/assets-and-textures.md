---
title: アセットとテクスチャ
description: モデル、モーション、相対パスのテクスチャを安全に読み込む方法です。
---

URL・ローカルファイル・ドラッグ＆ドロップの各入力から MMD アセットを扱います。

## 入力形式

`string`、`File`、`ArrayBuffer`、`Uint8Array` を使い分ける基準を説明します。

## テクスチャの解決

モデル内の相対パスを、配信 URL またはローカルファイルへ対応付けます。

## 読み込み失敗の扱い

欠落テクスチャとネットワークエラーを利用者へ分かりやすく通知します。
