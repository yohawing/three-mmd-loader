---
title: WebGPU / TSL
description: 実験的な TSL パイプラインで WebGPU または WebGL 描画を行います。
---

`createMmdTslPipeline` を使い、MMD モデル用の TSL マテリアルと描画処理を構成します。

## パイプラインを作成する

レンダラーとライトを渡して、モデル読み込み用の設定を取得します。

## モデルを接続する

読み込んだモデルをパイプラインへ attach して描画対象にします。

## 実験的機能の扱い

Three.js 側の API 変更に追随するための注意点を説明します。
