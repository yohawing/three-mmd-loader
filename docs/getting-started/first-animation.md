---
title: 最初のアニメーションを再生する
description: VMD をモデルへ設定し、フレームごとに更新します。
---

VMD を読み込んでモデルへ設定し、レンダーループで `model.update()` を呼び出します。

## VMD を読み込む

モーション、カメラ、ライトを含む VMD データをアニメーションとして取得します。

## モデルへ設定する

モデルとモーションの対応を確認してからアニメーションを設定します。

## レンダーループで更新する

経過秒数を渡してポーズ、モーフ、物理を同期します。
