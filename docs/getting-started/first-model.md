---
title: 最初のモデルを表示する
description: PMX または PMD を読み込み、Three.js のシーンへ追加します。
---

`ThreeMmdLoader` でモデルを読み込み、`model.root` をシーンへ追加します。

## ローダーを作成する

テクスチャやランタイムの設定を必要に応じてローダーへ渡します。

## モデルを読み込む

URL、ArrayBuffer、File などの入力からモデルを読み込みます。

## シーンへ追加する

読み込んだルートオブジェクトを Three.js の Scene に追加します。
